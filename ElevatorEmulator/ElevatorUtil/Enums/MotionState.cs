namespace ElevatorEmulator.ElevatorUtil.Enums
{
    /// <summary>
    /// Enum that represents the state of motion for the current elevator (Stopped or Moving). 
    /// </summary>
    public enum MotionState
    {
        Stopped,
        Moving
    }
}
